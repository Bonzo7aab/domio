# Local Supabase Test Environment Variables
# Copy this file to .env.test.local and fill in values from `supabase start` output

# Local Supabase Configuration
# Get these values by running: npm run supabase:start
# The output will show all connection details

NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_local_anon_key_here
# Elevated server key — set one of these (RLS bypass for tests / admin flows):
# SUPABASE_SECRET_KEY — Dashboard secret key (sb_secret_...), or
# SUPABASE_SERVICE_ROLE_KEY — legacy JWT from `supabase start` / old API tab
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=your_local_service_role_key_here

# Playwright Test Configuration
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Instructions:
# 1. Run: npm run supabase:setup (first time only)
# 2. Run: npm run supabase:start
# 3. Copy the API URL, anon key, and service_role key from the output
# 4. Paste them into this file (save as .env.test.local)
# 5. Run: npm run supabase:migrate-local (to apply database migrations)
# 6. Run: npm run test:e2e

