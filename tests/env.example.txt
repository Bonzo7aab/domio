# Test Environment Variables Example
# Copy this content to .env.test.local and fill in your values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here

# Supabase Service Role Key (required for test user cleanup)
# This key bypasses Row Level Security - keep it secure!
SUPABASE_SECRET_KEY=your_secret_key_sb_secret_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Playwright Test Configuration
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Set to true when running isolated E2E against `supabase start` (local :54321).
# When false/unset, Playwright dev server and seeded-user cleanup use Supabase from .env.local.
# E2E_USE_LOCAL_SUPABASE=true

# Skip deprecated pool-user setup for seeded-account specs (auto-enabled for manager-contest-creation)
# E2E_SKIP_POOL_USERS=true

# Optional: pre-seeded manager for manager E2E tests (defaults to zarzadca3@openpro.pl)
E2E_SEEDED_MANAGER_EMAIL=zarzadca3@openpro.pl
E2E_SEEDED_MANAGER_PASSWORD=Test1!

# Optional: pre-seeded contractor for verification E2E tests (defaults to wykonawca3@openpro.pl)
E2E_SEEDED_CONTRACTOR_EMAIL=wykonawca3@openpro.pl
E2E_SEEDED_CONTRACTOR_PASSWORD=Test1!



